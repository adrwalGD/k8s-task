{{- if .Values.currencyservice.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.currencyservice.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.currencyservice.name }}
spec:
  replicas: {{ .Values.currencyservice.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.currencyservice.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.currencyservice.name }}
    spec:
      serviceAccountName: {{ .Values.currencyservice.name }}
      securityContext:
        {{- toYaml .Values.global.podSecurityContext | nindent 8 }}
        {{- with .Values.currencyservice.podSecurityContext }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecretName }}
      containers:
        - name: {{ .Values.currencyservice.name }}
          image: "{{ .Values.currencyservice.image.repository }}:{{ .Values.currencyservice.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 7000
          env:
            - name: PORT
              value: "7000"
            - name: DISABLE_PROFILER
              value: "1"
          resources:
            {{- toYaml .Values.currencyservice.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.global.containerSecurityContext | nindent 12 }}
            {{- with .Values.currencyservice.containerSecurityContext }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          readinessProbe:
            grpc:
              port: 7000
          livenessProbe:
            grpc:
              port: 7000
{{- end }} 